
Not a command set itself, but important:

### NVMe 2.0+ introduces:

- **Command Set Identifier (CSI)**
- Each I/O queue is bound to a specific command set

👉 This allows:

- Multiple command sets on one device
- Flexible storage models